const express = require("express");
const { authenticate } = require("../middlewares/authMiddleware");
const router = express.Router();

// Define user-specific routes here, e.g., reporting unsafe areas

module.exports = router;
